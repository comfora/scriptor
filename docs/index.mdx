---
title: "Welcome to Scriptor"
description: "Open-source CLI tool designed for simple management of your local and cloud environments."
sidebarTitle: "Introduction"
mode: "wide"
---

Ranging from simple installs of VPNs or proxies to complex security configurations, Scriptor is designed to be a simple and easy to use tool for managing your environments. All in one installable python package.

{/* <Danger>
**Next Documentation**

Documentation for Scriptor is currently being written and may not be available for a while.
</Danger>

## Installation

Scriptor is available as an installable python package on PyPI and <Icon icon="python" iconType="solid" /> **Python 3.12 or higher** is required for usage.

<Tabs>
  <Tab title="Latest Release" icon="download">
    To install the latest stable release of Scriptor, run the following command:

    ```bash
    pip install scriptor
    ```

  </Tab>
  <Tab title="Specific Releases" icon="warning">
    To install a specific version of Scriptor listed on our [GitHub Releases](https://github.com/comfora/scriptor/releases), use the following command, replacing `x.y.z` with the desired version number:

    ```bash
    pip install scriptor==x.y.z
    ```
  </Tab>
</Tabs> */}
